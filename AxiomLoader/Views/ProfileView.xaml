<UserControl x:Class="AxiomLoader.Views.ProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="500">
    <StackPanel Margin="20">
        <TextBlock Text="User Profile" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>
        
        <Border Background="#1a1a1a" Padding="20" CornerRadius="10" BorderBrush="#333" BorderThickness="1">
            <StackPanel>
                <Grid Margin="0,0,0,15">
                    <TextBlock Text="Username:" Foreground="#b3b3b3"/>
                    <TextBlock Text="admin" Foreground="White" HorizontalAlignment="Right" FontWeight="Bold"/>
                </Grid>
                <Separator Background="#333" Margin="0,0,0,15"/>
                <Grid Margin="0,0,0,15">
                    <TextBlock Text="Subscription:" Foreground="#b3b3b3"/>
                    <TextBlock Text="Lifetime" Foreground="{DynamicResource PrimaryColorBrush}" HorizontalAlignment="Right" FontWeight="Bold"/>
                </Grid>
                <Separator Background="#333" Margin="0,0,0,15"/>
                <Grid Margin="0,0,0,15">
                    <TextBlock Text="Remaining Days:" Foreground="#b3b3b3"/>
                    <TextBlock Text="∞" Foreground="#00ff88" HorizontalAlignment="Right" FontWeight="Bold"/>
                </Grid>
                <Separator Background="#333" Margin="0,0,0,15"/>
                <Grid>
                    <TextBlock Text="HWID:" Foreground="#b3b3b3"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock x:Name="HwidText" Text="LOADING..." Foreground="#ff0055" FontSize="10" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <Button Content="COPY" FontSize="8" Padding="5,2" Background="#333" Foreground="White" BorderThickness="0" Click="CopyHwid_Click" Cursor="Hand"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
