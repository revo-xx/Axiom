<UserControl x:Class="AxiomLoader.Views.InjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="500">
    <StackPanel Margin="20">
        <TextBlock Text="Available Cheats" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>
        
        <!-- CS2 -->
        <Border Background="#1a1a1a" Padding="15" CornerRadius="5" Margin="0,0,0,10" BorderBrush="#333" BorderThickness="1">
            <Grid>
                <StackPanel>
                    <TextBlock Text="Counter-Strike 2" Foreground="White" FontWeight="Bold"/>
                    <TextBlock Text="Status: Undetected" Foreground="#00ff88" FontSize="10"/>
                </StackPanel>
                <Button Content="LOAD" HorizontalAlignment="Right" Width="70" Height="25" Click="LoadButton_Click" Tag="CS2" Background="{DynamicResource PrimaryColorBrush}" Foreground="White" BorderThickness="0" FontWeight="Bold" Cursor="Hand"/>
            </Grid>
        </Border>

        <!-- Valorant -->
        <Border Background="#1a1a1a" Padding="15" CornerRadius="5" Margin="0,0,0,10" BorderBrush="#333" BorderThickness="1">
            <Grid>
                <StackPanel>
                    <TextBlock Text="Valorant" Foreground="White" FontWeight="Bold"/>
                    <TextBlock Text="Status: Use with Caution" Foreground="#ffcc00" FontSize="10"/>
                </StackPanel>
                <Button Content="LOAD" HorizontalAlignment="Right" Width="70" Height="25" Click="LoadButton_Click" Tag="Valorant" Background="{DynamicResource PrimaryColorBrush}" Foreground="White" BorderThickness="0" FontWeight="Bold" Cursor="Hand"/>
            </Grid>
        </Border>

        <!-- Minecraft -->
        <Border Background="#1a1a1a" Padding="15" CornerRadius="5" Margin="0,0,0,10" BorderBrush="#333" BorderThickness="1">
            <Grid>
                <StackPanel>
                    <TextBlock Text="Minecraft" Foreground="White" FontWeight="Bold"/>
                    <TextBlock Text="Status: Undetected" Foreground="#00ff88" FontSize="10"/>
                </StackPanel>
                <Button Content="LOAD" HorizontalAlignment="Right" Width="70" Height="25" Click="LoadButton_Click" Tag="Minecraft" Background="{DynamicResource PrimaryColorBrush}" Foreground="White" BorderThickness="0" FontWeight="Bold" Cursor="Hand"/>
            </Grid>
        </Border>
    </StackPanel>
</UserControl>
